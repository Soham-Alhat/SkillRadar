name: Test Workflow

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        run: git clone https://github.com/${{ github.repository }} .

      - name: Print Hello
        run: echo "Hello Soham"